# Public browser build variables. Vite embeds VITE_* values into client code.
# This is safe to expose, but do not put auth tokens or other secrets here.
VITE_SENTRY_DSN="https://public-key@o0.ingest.sentry.io/0"
