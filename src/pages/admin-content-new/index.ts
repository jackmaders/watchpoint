/**
 * Public API for the new VOD creation page slice in the admin dashboard.
 *
 * Re-exports `adminContentNewRouteOptions` and `AdminContentNewRouteComponent` for creating and seeding
 * new VOD training sessions within the Feature-Sliced Design architecture.
 */
export { adminContentNewRouteOptions } from "./model/route-options";
export { AdminContentNewRouteComponent } from "./ui/admin-content-new-route";
