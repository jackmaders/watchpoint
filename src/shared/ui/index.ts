/**
 * Public barrel export for the shared UI component library, exposing reusable design system primitives.
 *
 * Centralizes consumption for higher Feature-Sliced Design layers, exporting the foundational `Button` component
 * and `buttonVariants` styling utility.
 */

export type { ButtonProps } from "./button";
export { Button, buttonVariants } from "./button";
