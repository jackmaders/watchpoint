Activate the `grilling` skill and interrogate the maintainer about this issue until the
frontier is empty. Number each question and offer a recommended answer.

This is a stub — the full prompt (issue context, prior rounds, frontier tracking) lands
with the ticket that wires this stage's runner.

{{OUTPUT_SCHEMA}}
