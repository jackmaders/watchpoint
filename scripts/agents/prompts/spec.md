Activate the `to-spec` skill and synthesise the settled grill conversation into a
specification, naming the test seams implementation and review will both work against.

This is a stub — the full prompt (grill history, domain docs) lands with the ticket that
wires this stage's runner.

{{OUTPUT_SCHEMA}}
