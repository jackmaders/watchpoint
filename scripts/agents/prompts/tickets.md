Activate the `to-tickets` skill and break the published spec into vertical slices, each
demoable on its own, wiring blockers between tickets in this same breakdown only.

This is a stub — the full prompt (spec body, existing child tickets) lands with the
ticket that wires this stage's runner.

{{OUTPUT_SCHEMA}}
