Activate the `research` skill and investigate the question below against high-trust
primary sources, citing each one.

The workflow supplied the decision ticket conversation below. Resolve its question
without changing repository files or answering any human-only decision. Put the
findings in `findingsMarkdown`, cite claims with links to primary sources, and do
not add a separate Sources heading; the workflow appends the validated source list.

Decision ticket conversation:

{{CONVERSATION}}

{{OUTPUT_SCHEMA}}
