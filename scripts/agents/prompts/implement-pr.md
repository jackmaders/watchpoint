Activate the `implement` skill and address the review findings on this PR's existing
branch, replying to each thread you resolve.

This is a stub — the full prompt (PR diff, review comments) lands with the ticket that
wires this stage's runner.

{{OUTPUT_SCHEMA}}
