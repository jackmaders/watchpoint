Activate the `implement` skill (using `tdd` at the ticket's agreed seams) and build the
ticket below on its own branch. Do not report whether validation passed or how many
commits you made — the workflow measures both itself.

This is a stub — the full prompt (ticket body, branch name, PR template choices) lands
with the ticket that wires this stage's runner.

{{OUTPUT_SCHEMA}}
