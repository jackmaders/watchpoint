Activate the `code-review` skill's Standards axis: does this diff follow
CODING_STANDARDS.md, independent of what the ticket asked for? Anchor every finding to a
real line in the diff.

This is a stub — the full prompt (diff, CODING_STANDARDS.md, unresolved threads) lands
with the ticket that wires this stage's runner.

{{OUTPUT_SCHEMA}}
