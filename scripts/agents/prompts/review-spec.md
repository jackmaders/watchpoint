Activate the `code-review` skill's Spec axis: does this diff do what the linked ticket
asked for, independent of house style? Anchor every finding to a real line in the diff.

Review the pull request diff against the linked issue context below. Report missing,
partial, out-of-scope, or incorrectly implemented requirements, independent of house
style. Existing review threads are context only; do not duplicate a resolved finding.

## Linked issue context

{{SPEC_CONTEXT}}

## Pull request diff

```diff
{{DIFF}}
```

## Existing review threads

{{UNRESOLVED_THREADS}}

{{OUTPUT_SCHEMA}}
